class CommandController:

    def get_input(self):
        command = input()
        return command
    def start_game(self):
        command = get_input()
        while (True):
            if command[0] == "new":

            if command[0] == "name":
            if command[0] == "show":
            
            #move A1 A2
            if command[0] == "move":
            if command[0] == "save":
            if command[0] == "load":
